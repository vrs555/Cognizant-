# Set custom server port
server.port=8083

# Optional: Logging level for debugging
logging.level.org.springframework=INFO

# Optional: Show full error messages in response (for development only)
server.error.include-message=always
